import { EstadoDirective } from './estado.directive';

describe('EstadoDirective', () => {
  it('should create an instance', () => {
    const directive = new EstadoDirective();
    expect(directive).toBeTruthy();
  });
});
